// Preload script for Electron
// This script runs before the web page is loaded
// It has access to Node.js APIs while the renderer process doesn't

window.addEventListener('DOMContentLoaded', () => {
    // Any preload initialization can go here
    console.log('POS-GIHON Desktop App loaded');
});
